# 授时作品浮窗首帧完整性与性能修复审计

- 日期：2026-08-13（Asia/Shanghai）
- 状态：本地实现与门控通过；提交、推送和生产部署证据在发布后补写
- 修复前可回滚基线：`8cf368c1dfd7549dc6f2c845c2b6d6ee29407240`

## 1. 复现结论

问题不是单一网络故障，而是三项因素叠加：

1. 作品 iframe 在浮窗静态外壳完成首帧绘制前启动，跨域脚本、Canvas、GIF 与媒体初始化会和外层页面争用主线程及合成资源。
2. 被遮住的日程详情仍持续绘制大量卡片和动画；浮窗与面板又各有一层大面积 `backdrop-filter`，提高 Chromium 合成与分块栅格化压力。按钮是外层静态 DOM，因此它们和作品下半部一起“消失”指向父级绘制/裁切问题，不是按钮网络加载失败。
3. 桌面宽度规则覆盖了既有上限；1280×720 时较长摘要把两个出口推到内部滚动区，4K 时 16:9 舞台被放大到不必要的尺寸。不同日期摘要长度不同，因此表现为偶发。

## 2. 已实施修复

- 先显示完整静态外壳、作品摘要和两个出口，再延后两帧启动 iframe；若浮窗先关闭，会取消延迟启动。
- 对作品源域增加 `preconnect` 与 DNS 预取；慢载状态仍只覆盖作品舞台，不能遮住摘要和出口。
- 浮窗打开时暂停底层日程的绘制，关闭后恢复同一滚动位置和焦点。
- 移除浮窗全屏层和面板的大面积磨砂滤镜，以不透明材质和原有描边、阴影维持视觉层级。
- 桌面腔体由视口宽高共同约束，最大 `1500×920`；1280×720 仍接近全屏，1920 和 4K 保持大幅作品而不无限放大。
- 桌面端两个出口始终在首屏；极长的四段摘要只在 `Brief / 作品摘要` 卡片内部滚动。移动端和短触屏继续使用一个自然的浮窗滚动根。
- 每次打开都会把面板和摘要重置到第一段；全屏退出、二次关闭、Escape、焦点恢复与 iframe 卸载层级保持不变。

## 3. 门控修复

`qa_timetable_dialog_full_artwork.mjs` 现在额外执行 2 秒延迟 iframe 情景，并断言：

- 延迟期间静态外壳、加载状态、四段摘要及两个出口均已存在；
- 底层日程暂停绘制，浮窗与面板没有 `backdrop-filter`；
- 桌面与 4K 出口位于首屏，4K 宽度有界；
- 关闭后底层日程恢复；重新打开时摘要从顶部开始；
- 原有四视口、16:9、按钮对齐、全屏返回、Escape、焦点及 iframe 卸载测试继续通过。

`qa_timetable_artwork_modal_touch_hints.mjs` 不再要求 4K 无限制放大，而是要求视口感知的有界尺寸、桌面无面板内部滚动、两个出口在首屏和作品舞台不低于可读尺寸。

Codex 与 Hermes 的 `granted-hours-art-development` 技能副本已经同步为同一字节内容。06:35 闭环后续仍会在本地、生产自定义域、稳定 Pages 域和不可变部署上运行上述门控；任一失败不得写成 `ok`。

## 4. 本地审计证据

- `npm run build:timetable`：通过；100 个公开日期；语义清洗 `changed=0`。
- 作品浮窗完整门控：普通桌面、390×844、421×386、3840×2160、2 秒慢载、全屏/关闭/Escape/焦点/卸载全部通过。
- 浮窗/触控几何：1280、1920、4K、移动与短触屏通过；桌面面板 `scrollHeight == clientHeight`，两个出口均在首屏。
- 月历富化、AI 作品卡、预览失败恢复、真实月历、统一时间线、公开层级与隐私 fixture：通过。
- 作品嵌入媒体握手：通过。
- 全作品语料：93/93 页面通过，92 个 Canvas 页面，0 page errors，0 local HTTP failures。
- 视觉预览：93 项、186 个镜像文件通过，OCR 可读界面文字命中 0。
- 公开安全扫描、第一人称公开契约、发布卫生与 `npm audit`：通过；生产依赖漏洞 0。
- 发布证据：确定性数据与 bundle 构建逐字节一致，`docs/timetable` 与标准构建完全一致。

专用 Chrome DevTools Performance 接口在当前会话未配置，因此本轮没有伪造 Lighthouse/Core Web Vitals 数字；改用真实浏览器视觉检查、资源预连接、受控慢载/失败注入、跨视口几何和全语料运行门控作为可重复证据。

## 5. 可回滚性

- 本轮代码、构建产物、门控与本报告将组成单一功能提交；可用普通 `git revert` 回滚，不改写历史。
- 修复前基线 `8cf368c1dfd7549dc6f2c845c2b6d6ee29407240` 保留。
- 不删除历史作品、日程或旧部署；Cloudflare 发布后会记录新的不可变地址，上一不可变部署仍保留。

## 6. 发布后补录

- 功能提交：待发布
- Cloudflare 部署 ID：待发布
- 不可变部署：待发布
- 自定义/稳定/不可变三端门控：待发布
